/**
 * @typedef MinimizedGroupSpec Minimized view container specs.
 */
type MinimizedGroupSpec = {containerId: string, corner: CompassCornerHeading, floating: boolean, height: number, width: number};
